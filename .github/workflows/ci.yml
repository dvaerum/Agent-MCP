name: CI

# TEMPORARY PROBE BRANCH — trivial stand-in for the real CI. Do not merge.
on:
  pull_request:

permissions:
  contents: read

jobs:
  trivial:
    name: Trivial PR check
    runs-on: ubuntu-latest
    steps:
      - run: echo "pull_request check"
