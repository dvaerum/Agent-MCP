name: Update flake.lock

# TEMPORARY PROBE BRANCH — neutered updater. Do not merge.
on:
  workflow_dispatch:

permissions: {}

jobs:
  probe-target:
    name: Dispatched check
    runs-on: ubuntu-latest
    steps:
      - run: echo "workflow_dispatch check"
