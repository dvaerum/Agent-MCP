# Moved

This document moved to
[`docs/integrations/api-versioning.md`](./integrations/api-versioning.md)
as part of the docs restructure into category folders.

This stub stays in place because the router emits a GitHub blob
URL pointing at the old path inside HTTP 406 error responses
(`agent_mcp/router/app.py::_API_DOCS_URL`). New documentation
links should point at the new location.
